# ============================================================
# requirements.txt -- ClawGUI-Eval direct dependencies
# Python 3.12
# ============================================================
# Core dependencies (directly imported by project code)
torch>=2.8.0
transformers>=4.57.0
qwen-vl-utils>=0.0.14
openai>=2.9.0
pillow>=12.0.0
tqdm>=4.67.0
numpy>=2.2.0
accelerate>=1.12.0

# -- Optional dependencies ----------------------------------------
# flash-attn: required when transformers backend uses attn_implementation="flash_attention_2"
# Install via: pip install flash-attn --no-build-isolation
# flash-attn>=2.8.1

# vllm: only needed when deploying API service via `vllm serve`
# vllm>=0.11.0
