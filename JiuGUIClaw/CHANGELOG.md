# CHANGELOG

每次完成或修改后在这里补一条,简单说明即可。按日期倒序,日期内按时间倒序。

条目末尾可用来源标注说明:
- **[合并 wxf]**:从 wxf 分支挑取/借鉴而来(通常是 `git show <wxf-hash>` 后手工 apply,避免 cherry-pick 拖入不想要的东西)
- **[自写]**:main 分支自己实现
- **[混合]**:wxf 有参考,但在 main 这边结合当前结构重写/调整

## 2026-04-30

- fix: 主题改克莱因蓝 + Memory 注入瘦身只留 long-term —— 同事推上来一笔混合改动,两件事一起做。**主题侧** `Theme.kt` 把 light / dark 整套调色板从「暖橙陶土 + 炭灰」整体翻面成「克莱因蓝(Klein Blue)+ 蓝灰中性」 —— light primary `#D97757 → #1C47C7`(Klein 主调),primaryContainer `#F4D4C7 → #DCE4FF` 浅蓝底配深蓝字 `#00174A`;background/surface 从奶油色 `#F8F4EE/#FCF8F2` 转到偏蓝灰 `#F5F7FC/#F9FAFF`,surfaceVariant 同步 `#EEE5DB → #E3E8F5`,outline 从暖灰 `#91857C` 转蓝灰 `#7B839C`;dark primary `#E28A66 → #8AA2FF`(柔化避免高对比蓝压迫),primaryContainer `#6B3B2D → #0D2F9F` 深蓝底,background `#2B2D38 → #111522` 偏蓝深灰,error 也从陶土橙 `#BF5547/#F08A73` 回标准 M3 红 `#B3261E/#FFB4AB`。文件顶部注释也同步重写「整体转向克莱因蓝气质,容器与背景降饱和避免整屏高对比蓝造成压迫感」。`ClawDrawerContent.kt` 底部用户卡的圆形图标底色和 tint 从写死 `Color(0xFFD97A57)` 那两套硬编码橙色去掉,改 `MaterialTheme.colorScheme.primaryContainer.copy(alpha=...)` + `MaterialTheme.colorScheme.primary`,跟着主题走;`SettingsScreen.kt` 的 `settingsIconContainerColor()` / `settingsIconTintColor()` 两个辅助函数同样改走 colorScheme,设置页七张子卡的圆形图标自然从橙变蓝。`MainActivity.kt` ChatScaffold TopAppBar 标题(sessionName)字号从 `titleLarge → titleSmall` —— 蓝主题下大字号会显重,小字号让顶栏视觉权重下沉,焦点回到对话气泡区。**Memory 侧** `AgentMemory.getMemoryContext()` 从「long-term + 最近 8 条 history」并列注入,精简为只注入 long-term:原来每次系统提示都把 HISTORY.md 末尾 8 条按 `## Recent History` 段塞进去,但 GUITool/MemoryConsolidator 写得勤,8 条很容易被几个 GUI 任务和压缩归档塞满,模型反而被这些事件流水占走 token,真正稳定的事实(MEMORY.md 里的姓名/偏好)被淹没;现在「Recent History」整段不再自动注入,留给 LLM 自己按需调 `read_memory(source="history")` 拿。提示词同步改:`ContextBuilder.kt` `read_memory` 描述补 "on demand" 修饰、Guidelines 显式新增一行「HISTORY.md is not preloaded into the prompt. If you need past event logs, call `read_memory` with source="history" or "both"」明确告诉模型不会自动看到、原"inspect the current conversation and Recent History first"两处改成"inspect the current conversation first. If that is not enough, use `read_memory` to inspect stored history"和"current conversation or stored history clearly shows..."。MEMORY.md 跨会话共享语义不动(workspace 是 App 级目录 `filesDir/workspace/memory/`,不是 per-session),稳定事实仍然每轮注入;只是事件流水从「自动给 8 条」变成「需要自己 grep」。改动统计:6 文件 +63 -73,主要是 Theme.kt 整套色值替换。需要观察:模型在不主动调 `read_memory` 时能否覆盖以往靠 Recent History 兜底的"刚刚做过 X 别再做"语义,真机使用一段时间后看实际命中。改动由同事推上来,本机不编译,真机由同事自验。(`4434b20`) — **[自写]**

## 2026-04-29

- docs: 新增 README_EN.md / SETUP_EN.md,顶部加双语切换 —— 主仓后续要面向英文受众,提前补一份英文文档。新增两份英文文档(共 +421 行):`README_EN.md`(126 行)按中文 README 当前结构对应翻译 —— H1 + 从属说明 italic + 三枚徽章 + 介绍段 + ✨ Highlights / 🚀 Quick Start / 🏗️ Architecture / 📖 Documentation / 🙏 Acknowledgements / 📄 License,同事在 EN 版补了 Current Capabilities 一节(列 nano/brain 工具集 + phone agent 可执行动作)和 Runtime flow ASCII 框图(`User → brain (LLM) → gui_execute → phone agent (VLM) → screenshot/action loop`),中文 README 现版本里没有这两节,后续要么补回中文版要么把英文版精简对齐;`SETUP_EN.md`(295 行)对应 SETUP.md 翻译。两份中文文档顶部各加一行语言切换链接:`README.md` 第 3 行加 `[English README](README_EN.md) | [中文安装指南](SETUP.md) | [English Setup Guide](SETUP_EN.md)`,`SETUP.md` 第 3 行加 `[中文 README](README.md) | [English README](README_EN.md) | [English Setup Guide](SETUP_EN.md)`,四份文档互链覆盖全;两份英文文档顶部也各有反向跳回中文版的链接行(由新增文件本身提供)。中英版本目前内容存在轻微 drift(EN 多 Capabilities + 流程图,内文措辞独立翻译不是逐字 mapping),后续如果有 README 改动要双向同步,这次先把英文版立起来,drift 的部分晚点和同事确认后再统一收口。无代码改动。改动由同事推上来。(`4403e09`) — **[自写]**

## 2026-04-27

- docs: README 致谢段精简到 Shizuku / roubao,删 architecture-notes 指引 —— 上一轮把致谢节按主仓 `ZJU-REAL/ClawGUI` 体例写成 4 条(nanobot / Shizuku / roubao / oapi-sdk-java),用户复审时质疑两点:(1) 主仓 ClawGUI 的致谢里已经致谢过 nanobot 了,我们仓再致谢一次构成重复 —— 在主仓视角下"nanobot 受益方"只需要在最上层声明一次,子项目不该向同一个上游再致谢一遍;(2) `oapi-sdk-java` 是飞书 SDK,通过 Maven 拉的 jar 进 build 链,代码并没有进我们仓库,本质和 OkHttp / Kotlinx-coroutines 一样是依赖,所有 Maven 依赖都不致谢的话,这个也不该单独拎出来致谢。统一原则定为"代码进了仓库的才致谢" —— `Shizuku` 通过 AIDL stub + `ShizukuProvider` 进 manifest,有实际代码集成;`roubao` 是 MIT 协议的 Android Kotlin 参考项目,我们 Shizuku 集成思路、悬浮球壳、IME broadcast 协议这几块借鉴了它的实现;两条都满足"代码进仓"标准。借此用户也复核了一下 `ClawguiIME.kt`(`core/ime/`,100 行),确认是从零写的 `InputMethodService` 子类,只借鉴了 ADBKeyboard 的 broadcast 思路(action 命名是我们自己的命名空间 `com.clawgui.android.IME_*`,ADBKeyboard 是 `ADB_INPUT_*`),不存在代码搬运,所以 ADBKeyboard 不进致谢节。同时删掉「架构概览」节末那句"详细的目录结构、技术栈、运行流程,以及修改前注意事项见 `docs/architecture-notes.md`" —— 用户提出 README 主索引收口到 SETUP 后,`docs/architecture-notes.md` 已经定位为"开发者内部文档"不对外暴露,这句指引和文档节只列 SETUP 的口径冲突;开发者通过仓库目录直接发现该文档,而 README 读者不需要看到它。改动:致谢从 4 条 → 2 条(只删 nanobot 和 oapi-sdk-java,Shizuku/roubao 原条目原样保留),架构概览段末删 1 行,共 -4 行。无代码改动。(`b778828`) — **[自写]**

- docs: README / LICENSE 向 ClawGUI 主仓子项目规格对齐 —— 上一轮把 README 索引收口到 SETUP 时同步意识到一件事:本仓后续整体会 merge 进 ClawGUI 主仓(`ZJU-REAL/ClawGUI`)成为 `clawgui-android/` 子目录,届时这份 README 会和兄弟 `clawgui-agent/README_CN.md` 平级展示,但当前体例完全不像主仓家族成员 —— 没有从属说明、致谢节里把"ClawGUI-Agent"当外部项目致谢、章节标题没 emoji 也没 LICENSE。这次按上游 [`README_zh.md`](../ClawGUI/README_zh.md) 和兄弟 [`clawgui-agent/README_CN.md`](../ClawGUI/clawgui-agent/README_CN.md) 的样板做规格对齐,merge 那一刻就不需要再大改:(1) H1 下加 italic 从属说明「ClawGUI-Android 是 [ClawGUI](../README_zh.md) 的真机部署模块,对应主仓路线图中的「真机部署 ClawGUI-Agent」」,链接路径 `../README_zh.md` merge 后才解析正确 —— 用户接受这个 trade-off,merge 前是预留死链。原表述路线图有"避免云端泄露隐私",但现状大脑/VLM 都走云 API,没法兑现这个承诺,所以引用时只截"真机部署 ClawGUI-Agent"这一截,不蹭后半句。(2) 加三个轻量徽章(Android 8.0+ / Apache-2.0 / Shizuku required),不堆主仓那种 5+ 徽章 + banner + 视频 + arXiv badge 的体量,真机端项目应该轻量。(3) 介绍段重写 —— 原版「用户在手机里发一句话,brain 负责理解任务和决策,phone agent 负责看屏幕、规划动作、执行点击/滑动/输入;执行过程会显示悬浮球状态,任务记录会落盘成 trace,外部 channel 消息也能接进来」是流水账,照 `clawgui-agent` 的「定位 → 技术基底 → 双能力分述」三句调子重写:第一句"整体搬到 Android 上独立运行,不再依赖双端架构"做定位、第二句"基于 Shizuku 提供的非 root 高权限通道构建,由两层 agent 驱动"做技术基底并第一次亮 brain/phone agent、第三句"手机端形态方面... 远程接入方面..."做能力分述。(4)「项目特点」节 bullet 内描述跟着调整避免和介绍段重复 —— 改用粗体短语 + 破折号 + 一句精炼说明,而不是冒号 + 长句。(5) 章节标题加 emoji `✨ 项目特点 / 🚀 快速开始 / 🏗️ 架构概览 / 📖 文档 / 🙏 致谢 / 📄 许可证`,跟主仓和兄弟项目一致。(6) 致谢节重写 —— 原表述把"ClawGUI-Agent 提供原始问题域和 agent 结构参考"作为第一条致谢,merge 进主仓后就变成"自家子项目致谢自家家长",尴尬;主仓的从属关系已经在顶部 italic 那行交代过,致谢节就只列真正的外部开源依赖:`nanobot`(brain 层 function-calling 结构上游,本项目 Kotlin 移植自 `clawgui-agent/nanobot/` 的 Python 实现)/ `Shizuku`(非 root 高权限)/ `roubao`(MIT,Android/Shizuku 集成借鉴)/ `oapi-sdk-java`(飞书 channel)。(7) 加「许可证」节 + 落 `LICENSE` 文件 —— 一字不改复制自主仓 `/pubdata/lihp/prj/JiuGui/ClawGUI/LICENSE`(Apache-2.0 标准全文,201 行,不填 Copyright holder,Apache-2.0 全文不要求填),merge 进主仓时协议天然一致,不存在子目录和主仓协议冲突。(8) 删 `CHANGES_FROM_ROUBAO.md` —— 用户提出"独立 md 没人会看,致谢节点名就够了",同意:借鉴清单里 14 个文件级映射在第一次接入时是有用的对照,但接入完成后这文件就成了"项目档案",和实际 `build.gradle.kts` 一旦脱节(之前已经发生过,所以上一轮把"依赖变化"段改成一行指回 `build.gradle.kts`),与其留个会过期的清单,不如让 `git log -- app/src/main/aidl/` 这类按需追溯回 commit 信息。CHANGELOG 历史 entry(`a023b7d`/`5fe518b`)里两处提到 `CHANGES_FROM_ROUBAO.md` 的字样保留不动,那是时间快照,不该回头改。改动总览:`README.md` 46 行 → 58 行(密度更高但结构清晰,徽章 / italic / 三句介绍 / 致谢条目都是新内容);新增 `LICENSE`(201 行 Apache-2.0);删 `CHANGES_FROM_ROUBAO.md`(31 行);roubao 仓地址用户给到 `https://github.com/Turbo1123/roubao`,致谢节里加上 link。无代码改动。(`97f4b0b`) — **[自写]**

## 2026-04-27

- docs: README 文档索引收口到 SETUP,其他文档不在 README 暴露 —— 上一轮 README 精简到 50 行后「文档」节还列了 6 条(SETUP / architecture-notes / feishu-setup / diagnostics / CHANGELOG / CHANGES_FROM_ROUBAO),其中 5 条都是开发/维护期资料,放在面向用户的 README 索引里其实是错位 —— 「想用 ClawGUI」的读者看不到 SETUP 反而被 architecture-notes / CHANGELOG 这些技术内幕分散注意。后续整体会 merge 进 ClawGUI 主仓开源,届时本仓 README 会变成子项目 README,深度文档会跟着主仓那边的文档体系统一组织,所以现在保留全部文档文件继续维护(architecture-notes 仍是「架构概览」节的链接目标 / CHANGES_FROM_ROUBAO 仍是「致谢」节的链接目标 / feishu-setup 仍由 Settings → Channels 提示链接 / diagnostics 仍由 About 子页诊断卡链接 / CHANGELOG 仍按日期倒序记账),只是不在 README 主索引重复列举。改动:`README.md` 「文档」节从 6 条 link 删到 1 条(只留 SETUP.md),其余文件不动;README 总长 50 → 45 行。无代码改动。(`477fb00`) — **[自写]**

## 2026-04-26

- docs: README 二轮精简到 OSS 标准体量,深度内容下沉 architecture-notes —— 上轮拆完 SETUP 后 README 仍有 210 行,「它能做什么 / 运行流程 / 当前能力 / 技术栈 / 飞书 channel / 项目结构 / 适合谁 / 项目状态 / 末尾阅读顺序」9 节属于自我介绍腔,普通 OSS README 不会写。这次按职责再砍:(1) README → 50 行,只留 标题 + description / 项目特点(4 bullet)/ 快速开始(5 步 + SETUP.md)/ 架构概览(2 行 bullet 描 nano + phone)/ 文档(6 条索引)/ 致谢。删掉「当前能力 / 适合谁 / 项目状态 / 末尾阅读顺序」—— 代码、SETUP、文档索引能说清楚的事不在 README 里讲第二遍;(2) `docs/architecture-notes.md` 升级为「ClawGUI Android 架构笔记」总入口,顶部加三节「项目结构」(目录树)/「技术栈」(依赖列表)/「运行流程」(ASCII 图)从 README 迁过来,原 H1「修改核心组件前的注意事项」改 H2「修改前注意事项」收成一大节,内部「先对照 Python 原版 nanobot」「两层 agent 的契约」「改 system prompt 之前」「Memory / History / Session 三层职责」「异常路径也要写进 Session」「Consolidation 阈值」「提交前自检清单」从原来的 `## 1-7` 一律降到 `###` 子节(去掉数字编号),内容一字不改。文件从 72 行 → 138 行,既给新人项目地图又留 review checklist;`build.gradle.kts` 链接路径从 `app/build.gradle.kts` 改 `../app/build.gradle.kts` 适配 `docs/` 子目录。无代码改动。(`7100a3d`) — **[自写]**

- docs: README 抽用户上手指南到独立 SETUP.md —— 原 README 478 行,项目介绍和详细用户安装步骤(环境要求 / Shizuku 三种启动 / 首次配置 / 常见问题)塞同一份长文档,既不像普通仓库 README,新读者也得翻过半屏 Shizuku 配对流程才看得到架构图。这次按职责拆:(1) 新增 `SETUP.md`(repo 根),300 行,迁五节流程性内容 ——「安装与配置」(开发者环境 + APK 编译 + 安装用户侧 + 开启开发者选项)、「Shizuku 启动方式」(root / 无线调试 / 电脑 ADB 三种)、「ClawGUI 首次配置」(授权 + provider/model + 输入法 + 第一次测试)、「推荐使用流程」、「常见问题」(4 个 FAQ),内容从 README 原样搬避免无意义 churn(标点/措辞照旧,后续要润再单独做),顶部一段「想了解 ClawGUI 是什么、能做什么、技术架构请先看 README」+ 底部「相关文档」反向跳回 README 和 docs/ 下的 feishu-setup / diagnostics / architecture-notes,SETUP 和 docs 互链不绕回 README 一次。(2) README 缩到 210 行,只保留项目介绍 + 索引 —— 删的 5 节正是迁出的;新加「快速开始」节放在「项目特点」之后、「架构概览」之前,给一条 5 步最短路径(装 Shizuku + ClawGUI → 启动 Shizuku → 首次授权 → 填 provider/model → 跑「打开微信」验证),关键链接 3 处指 SETUP.md(开头一段、第 2 步括号注、收尾段),保证新读者不会错过详细步骤,老读者也不必每次找架构图都翻过半屏 Shizuku 配对流程;「文档」节把 SETUP.md 列成第一条,跟 docs/ 平级;最末「建议阅读顺序」从 README → feishu-setup → architecture-notes → CHANGELOG 扩成 README → SETUP.md → feishu-setup → architecture-notes → CHANGELOG,5 步。纯文档结构,无代码改动。(`9bc5f43`) — **[自写]**

## 2026-04-24

- feat: 用户主动停走 Stopped 中性态,不再显示红色"出错" —— 同事反馈的"任务完成后点'再来一次'突然弹红色任务失败弹窗"看日志是假警报:`00:01:15 task1 Done → 00:02:02 task2 started → 00:02:20 stopSession requested by user → released as Done(text=已停止)`,日志完整,红色弹窗其实是 `1cc1ac0` 里 stopSession 复用 `ExecutionStatus.Error("已停止")` 的副作用 —— 当时想让主动停显示 ✗ 而不是 ✓ 区分自然完成,但借道 Error + AgentService 硬编码的 `"出错: ${status.message}"` 前缀,把"用户主动停止"渲染成了看起来像 crash 的"出错: 已停止"。这次把 user-stop 从 Error 语义里拆出来拿独立态:(1) `App.kt` 的 `ExecutionStatus` sealed 加 `data class Stopped(val message: String)`;`stopSession` 里原 `_executionStatus.value = ExecutionStatus.Error("已停止")` 改 `ExecutionStatus.Stopped("已停止")`,注释写清"中性结束不是 Error"。(2) `ui/overlay/AgentOverlayManager.kt` 的 `OverlayState` sealed interface 对应加 `data class Stopped(val text: String)`;`applyState` 里新分支:spinner GONE + icon `■`(和 ✓/✗ 走同一条 TextView,区分显著)+ 强制 Compact(和 Done/Error 一样走大矩形不走圆形 Mini);`backgroundResFor` 加映射走新增的 `res/drawable/overlay_bg_stopped.xml`(`#EB757575` 中性灰,和 Done 的绿 / Error 的红并列)。(3) `service/AgentService.kt` 的 `handleStatus` when 加 `is ExecutionStatus.Stopped ->` 分支:`overlay?.show(OverlayState.Stopped(status.message.take(48)))` + `updateNotification(status.message.take(48))` —— 注意**通知栏文案直接用 `status.message` 不拼"出错:"前缀**(Error 分支那两处才是红色语义的出处),auto-dismiss 沿用 `DONE_AUTO_DISMISS_MS = 3s` 而不是 `ERROR_AUTO_DISMISS_MS = 5s`(用户主动停是成功行为,不用留长时间让人读"出错信息")。消费点只有 AgentService 一处(grep `is ExecutionStatus` 确认零 UI 侧 when 依赖 `ExecutionStatus`,不会有漏网分支导致 warning 或非穷尽 when),其他路径对 Stopped 感知为"非 Running / 非 Error / 非 Done" 天然兼容。业务侧 `stopSession → releaseSlot → 后覆盖 _executionStatus` 的整条链路不动,slot/pendingTurns/trace 记录不变,只换了状态枚举叶子和 overlay 呈现。视觉:同事之后再手动停止任务,悬浮球会出 `■ 已停止` 灰色卡片 3s 后自动消失,通知栏也只显示"已停止"两个字,不会再被当作 crash 报 bug。(`ae572ef`) — **[自写]**

- fix: 导航重构后首次真机编译两处报错 + 设置子页 IME 遮挡 —— 同事在电脑上编 `1ccc966` 打出的包时暴露的问题,本机没安卓环境跑不了编译所以上一版没拦住。(1) `ClawDrawerContent.kt` 的三处 `HorizontalDivider` 在 Compose BOM `2023.10.01` 对应的 material3 `1.1.2` 里还没这个 API —— `HorizontalDivider`/`VerticalDivider` 是 1.2 才加的,现在项目其它地方用的都是老 `Divider()`,改回 `Divider` 和项目口径一致(deprecation warning 能接受,升 BOM 是独立动作不在这次范围);(2) `SettingsScreen.kt:913` "诊断" Card 的说明文字里 "开启`"`诊断模式`"`会多打..." 的内引号是 ASCII 半角双引号而不是中文双引号,Kotlin 把它当字符串提前闭合,后半句 `诊断模式` 变成了悬空标识符 → parser 报错;转义成 `\"诊断模式\"` 修复,Text 展示里照样是双引号。(3) IME 遮挡:同事反馈 AI 模型/Channels 这种长表单页里,base URL 或 API Key 输入框位于下半屏时键盘弹起会把当前行盖住,只看得见键盘看不见在输什么。根因 —— `AndroidManifest` 里 `windowSoftInputMode="adjustNothing"`(38d1a95 为避开 adjustResize 和 Compose 双计故意改的,Chat 页靠 ChatScreen 外层自己 `.imePadding()` 让位),本次 drawer 重构新加的 `SettingsScaffold` 内容 Box 没接上这层,Scaffold 不会自动收缩可视区 → IME 直接盖底半屏 → 光标不会被自动滚进视口。修法:只在 MainActivity `SettingsScaffold` 的 content Box 上加一次 `.imePadding()`(foundation.layout 的),7 处 SubPageScaffold/SettingsIndex 不用逐个改;IME 弹起 → Box 底部往上收 → 内层 `verticalScroll` Column 的可视区变短 → Compose 的聚焦输入框 `bringIntoView` 机制会把光标自动滚进视口。`ChatScaffold` 保持不动(ChatScreen 内部已 `.imePadding()`,双计反而会多出 bottom gap),`InboxScaffold` 无输入控件也不动。(`23788c1`) — **[自写]**

- feat: 导航重构 —— bottom tab 换 left drawer,Inbox/Settings 收进覆盖层 —— 原 4-tab 底栏(Chat/History/Inbox/Settings)属于经典 messenger 骨架,但实际 95% 场景是 Chat,另外三个各占一级 tab 是浪费视觉权重;参考 Qwen/Claude/ChatGPT 通用的 chat-first + left drawer 心智改造。(1) 新增 `ui/screens/ClawDrawerContent.kt`:`ModalDrawerSheet` 内从上到下 —— 「＋ 新建对话」FilledTonalButton(`!isExecuting` 时启用)→ 「📨 外部会话」NavigationDrawerItem(点推 Inbox 覆盖层)→ HorizontalDivider + 「历史会话」section label → LazyColumn 跑 `app.sessions.listSessions().filter { key.startsWith("ui:") }` 的 `ui:*` session 列表(长按弹 ModalBottomSheet:重命名 / 删除,沿用原 HistoryScreen 的 `pendingMenu`/`pendingRename`/`pendingDelete` 三 state 交互,复用 `RenameSessionDialog`)→ 底部"ClawGUI · 设置 ›"用户卡(圆形 `primaryContainer` 底 + `AutoAwesome` icon + 版本号副标 + `ChevronRight`,点推 Settings 覆盖层)。外部 `feishu:*` session 留给"外部会话"入口,不污染历史列表的尺度感。(2) `ui/MainActivity.kt` 大改骨架:原 `sealed class Screen{Chat,History,Inbox,Settings}` + bottomBar `NavigationBar` 全删,换成 `enum class OverlayScreen{Settings,Inbox}` + `ModalNavigationDrawer(drawerState, gesturesEnabled = overlay == null, drawerContent = { ClawDrawerContent(...) }) { when(overlay) { null -> ChatScaffold; Settings -> SettingsScaffold; Inbox -> InboxScaffold } }`,三个 Scaffold 作为 MainActivity 内的 `private @Composable` 就近定义,各自自带独立 TopAppBar:ChatScaffold 左 `≡` hamburger(`scope.launch { drawerState.open() }`)+ 中 sessionName + 右 ✏️ Rename(enabled = messages.isNotEmpty()),原"新建会话"➕ icon 迁到 drawer 顶部那颗按钮;SettingsScaffold / InboxScaffold 左 ← Back + 中标题。Back 键优先级三档互斥 BackHandler —— drawer 开 → 关 drawer / Settings 子页 → `tryBackFromSubPage()`(沿用原 dirty check + AlertDialog) / overlay 非子页 → `overlay=null` 回 Chat。`gesturesEnabled = overlay == null` 只让 Chat 层允许从左划开抽屉,避免 Settings/Inbox 的 subpage 返回和抽屉手势打架。(3) `ui/screens/InboxScreen.kt` 保留原有数据层不动,MainActivity 把 `InboxScaffold.onPicked` 接成 `{ overlay = null }`,点外部消息 → `app.switchSession(key)` → 覆盖层关 → 落回 Chat。(4) 删 `ui/screens/HistoryScreen.kt` 整文件,功能 100% 迁到 drawer(grep 过 `HistoryScreen(` 只有 MainActivity 一处引用,改完清零);README 的功能列表同步换成「ClawDrawerContent / InboxScreen:左抽屉历史会话(长按重命名/删除) + 外部 channel 会话覆盖层」。(5) 顺手修 dark 模式 container 对比度瑕疵 —— 原来 `primaryContainer=#3A1F1D`/`secondaryContainer=#2A2A2E` 贴近 `background=#1A1A1D`,设置页的圆形图标底、drawer 选中行几乎看不见;现在 `background #1A1A1D→#14141A`(和 surface `#1A1A1F` 做一点层级区分,让 TopAppBar/Scaffold 边界可辨)、`primary #C84A3D→#E38575`(同色相偏亮对 dark 友好,`onPrimary` 同步改深色 `#2F0C09` 避免白字红底刺眼)、`primaryContainer→#5A2A24`、`secondaryContainer→#3A3A40`(drawer 选中行/聊天气泡对比明显)、`tertiary` 同理提亮、`onSurfaceVariant #8A8A90→#B5B5BA`(supporting 文字/菜单图标读得清)。light 调保留 `#FFFBF8` cream + `#C84A3D` 红砖不动。业务逻辑零改 —— `App.kt` StateFlow / `newSession`/`switchSession`/`renameSession`/`deleteSession`/`stopSession`/`sendInstruction` 签名不动,`ChatScreen`/`SettingsScreen`/`RenameSessionDialog`/`OnboardingScreen` 内容零改,`AgentOverlayManager`/`AgentService`/slot 语义/诊断日志全部不受影响。带着两个 revert(回退 f8c8574+d048d45 那次方向偏了的视觉分组改造)一起推出去,让远程历史回到 abfb293 正轨再叠上本次重构。(`1ccc966`) — **[自写]**

## 2026-04-23

- feat: 诊断日志统一出口 + 一键导出按钮(开发调试用) —— 本机没安卓环境,真机 bug 只能靠同事复现后拉日志回来,先前代码里 `java.util.logging.Logger` 标签杂乱、`adb logcat` 捞不齐。这次收口成一个对外接口 + 同事零门槛的导出入口,主要是为后续 "Done 状态迟迟不出现 / 点停止才弹" 这类时序 bug 复现后能拿到完整现场。(1) 新建 `core/util/Log.kt`:`android.util.Log` 的薄封装,所有 tag 强制带 `ClawGUI/` 前缀(`adb logcat -d \| grep ClawGUI` 一把梭),每条日志强制带 `sessionKey`(可为 null),跨线程/跨 channel 能对上同一笔 turn;暴露 `Log.diag` 标志 + `setDiagEnabled(...)`,以后 "诊断模式下打更多细节" 的调用点用 `if (Log.diag) Log.i(...)` 守一下。`SettingsStore` 加 `diagnosticMode` 布尔 prefs(默认关),`App.onCreate` 里读出来初始化 `Log.setDiagEnabled`。(2) 关键节点埋点(I 级常开,不分 diag):`App.acquireSlot / releaseSlot / stopSession` 打 slot 变化 + result 类型 + 被占/跳过路径;`AgentLoop.dispatch` 入口打 channel/chat,出口打耗时 + 是否 publishOutbound,catch 分支打 cancelled/failed + 耗时(原有 `recordError` 保留不动);`AgentLoop.cancelSession` 打取消掉几个 active job;`ChannelDispatcher.startDispatch` 打 outbound loop 起停 + 每条 msg 的 `→ ch.send (len=N)` 和 `✓ ch.send done (Nms)` / `✗ ch.send failed`,未知 channel 警告带 sessionKey;`InAppChannel.send` 进/出 + 耗时 + isCurrent,缺 sessionKey / 无 pending turn 两个早退路径都打警告(直接定位 "消息到了但 UI 没更新")。(3) 新建 `core/util/LogExporter.kt`:`ProcessBuilder("logcat", "-d", "-v", "time")` 一次性快照 logcat 环形缓冲区,只保留带 `ClawGUI/` 的行,截到最近 5000 行,落到 `workspace/diagnostics/log_<ts>.txt`;`buildShareIntent` 走 `FileProvider` 扔给系统分享选择器;`clearOldLogs` 清老文件。`res/xml/file_paths.xml` 给现有 FileProvider 注册 `workspace/diagnostics/` 路径(authority 沿用 `${applicationId}.fileprovider`)。(4) 设置 → 关于 子页底部加 "诊断" Card:诊断模式 Switch(勾选立刻写 prefs + 热刷 `Log.setDiagEnabled`)+ "导出诊断日志" 按钮(触发 `exportToFile` → Toast 文件名大小 → 弹 Share Intent)+ "清空旧日志"。UI 入口写死在 About 页,上线前删掉整张 Card 即可(`docs/diagnostics.md` 写了回滚步骤)。(5) 新增 `docs/diagnostics.md`:同事侧使用步骤(打开诊断 → 复现 → 导出 → 分享)+ 开发侧 tag 速查 + 按 sessionKey grep 的排查套路。现有行为零变化,所有埋点只是多打日志,不改状态流转;FeishuChannel / TraceRecorder / Bus / overlay 全部不动。(`abfb293`) — **[自写]**

- feat: VLM 设置卡标注 adapter 白名单 + 非白名单模型警告 —— 之前 VLM Card 的 Provider 下拉走 `ProviderRegistry.PROVIDERS` 的 15 家 provider,但 `core/phone/model/adapters/` 只实现了 5 个 adapter(AutoGLM / UI-TARS / Qwen-VL / MAI-UI / GUI-Owl),模型名正则匹不上会 fallback 到 `AutoGLMAdapter` —— 不崩但 prompt 模板和响应解析按 AutoGLM 口径走,用其他 VLM 会静默跑偏。UI 上不拦也不提示,用户误以为"选项都能用"。只加告示牌不动匹配逻辑:(1) `ProviderCard` 加两个可选参数 `subtitle: String?` / `vlmAdapterWhitelist: Boolean = false`,Brain Card 全默认值不动;(2) VLM Card 标题下加灰字 subtitle「已适配:AutoGLM / UI-TARS / Qwen-VL / MAI-UI / GUI-Owl。选用其他模型将按 AutoGLM 模板解析,效果不保证。」;(3) Model 输入框判断 `vlmAdapterWhitelist && detectModelType(model) == "autoglm" && !model.contains("autoglm")` 时 `supportingText` 显示"实验性:未匹配已适配的 VLM,将按 AutoGLM 模板解析" + `isError = true`(M3 自动走 error 色描边)。`detectModelType` 复用 `AutoGLMAdapter.kt` 的现成正则,和实际 adapter 选择口径完全一致,不会出现"UI 说支持但实际 fallback"或反之。不改白名单(下拉框所有 provider 全留),保留接新 VLM 的出口。(`a9e42f0`) — **[自写]**

- fix: 悬浮球 Done 切换延迟 —— AgentService `collectLatest` 守卫 + overlay `applyState` 等值跳过。用户反馈「任务完成了但悬浮球要转很久才切到绿色 ✓」,属于累积型变慢(冷启动一次不明显,跑过几次后肉眼可感)。两个子因各修一半:(1) `AgentService.onStartCommand` 每次 `acquireSlot → startForegroundService` 都会叠一个新 `collectLatest` 订阅 —— startForegroundService 在服务已运行时仍会再次触发 onStartCommand,原代码直接 `scope.launch { executionStatus.collectLatest { handleStatus(...) } }` 没幂等守卫,跑 N 次任务后主线程每次状态变化要跑 N 遍 handleStatus(全是同一份状态),Done 切换被排在一长串重复 Running 后面显示迟缓。新增 `private var statusJob: Job?` 字段 + `if (statusJob?.isActive != true)` 守卫,`onDestroy` 里 `statusJob?.cancel()`;后续 onStartCommand 直接复用首次那一个订阅。(2) `AgentOverlayManager.applyState` 头部加 `if (state == lastState) return` 等值跳过 —— 一次任务周期 `show`/`update` 会被调几十次(每步 VLM 一次 onProgress + `tickJob` 每秒一次 + 可能的多重订阅叠加),绝大多数内容是同一份(tickJob 只刷新秒数、VLM step 也常落在相同 stageHint),不拦就每条都塞 `applyState + updateViewLayout` 进 main Handler 队列,撞上主线程拥堵时 Done 会被排在一长串 Running 之后。`lastState` 字段之前就用于"同值判等"补丁的目的只写不读,现在正式用起来。两处均不影响正确性:`OverlayState.Running(text, thinking, actionJson)` 是 data class 结构等值,tickJob 每秒变化的 `elapsed` 会让 `text` 不同天然通过,Done/Error 和 Running 永远不等值天然通过。(`21a82e3`) — **[自写]**

## 2026-04-22

- refactor: `ChannelDispatcher` 删掉 `startedJobs` 列表 + 精简注释 —— `a023b7d` 的 simplify 走查后发现:`startedJobs: MutableList<Job>` 是多余记账。`FeishuChannel.start` 把 WS 主循环委托给自己 `wsJob` 之后立刻返回,`InAppChannel.start` 直接一句 `running = true`,所以 `startChannels` 里 `scope.launch { ch.start() }` 的协程几乎瞬间完成 —— `stopChannels` 末尾的 `startedJobs.forEach { cancel() }` 每次都是在 cancel 已完成 Job,零收益。删 `startedJobs` 字段 + 两处 `.add` / `.forEach.cancel().clear()`,各 channel 的幂等(FeishuChannel 的 `if (running) return` 守卫 / InAppChannel 的 no-op)已经够用。顺手把 `stopChannels` 的 `catch (_: Exception) {}` 补成和 start 对齐的 `logger.warning("Channel ... stop failed: $e")`,`App.kt` 里叙述式的三行注释压成两行(KDoc 已经在 dispatcher 侧覆盖 why,App 侧不用再讲一遍)。行为不变,仅清洁。(`216b99e`) — **[自写]**

- fix: 应用内发完指令切出 App 后悬浮球一直转,必须回到 ClawGUI 才看到"已完成" —— 根因在 `App.initChannels` 的 `ProcessLifecycleOwner` observer:`onStop` 调的是 `ChannelDispatcher.stop()`,这个 stop 既断飞书 WS(本意),又把 `dispatchJob` 整个 cancel —— `dispatchJob` 是 bus outbound 的**唯一**消费循环,`InAppChannel.send` / `FeishuChannel.send` 都靠它派发。后台去抖 ~30s 过后 outbound 消费一停,AgentLoop 跑完 `publishOutbound` 产出的最终回复就挂在 bus 里没人取 → `InAppChannel.send` 不调 → `releaseSlot` 不调 → `_executionStatus` 卡 Running、悬浮球一直转;用户回到 ClawGUI → `onStart` → `dispatcher.start()` → 重启消费循环 → 这一刻才拿到消息调 release。修法:`ChannelDispatcher` 的 `start/stop` 拆成两档 —— `startDispatch()` 进程级常开(`App.onCreate` 里调一次,`dispatchJob` 跟进程同生共死),`startChannels/stopChannels` 跟前后台走(只管各 channel 自身的长连,例如飞书 WS);`dispatchJob` 的 `while (isActive)` 循环从 `startDispatch` 里启动,和 channel 启停解耦。App.kt 里 `ProcessLifecycleOwner` observer 相应改成 onStart 只 startChannels、onStop 只 stopChannels。`FeishuChannel.start` 第一行 `if (running) return` 幂等守卫保留不动,反复切前后台不会重开 WS;InAppChannel 的 start/stop 本就是 no-op,搭车零代价。`startedJobs` 只由 startChannels 攒、stopChannels 清,dispatchJob 不进这份表,互不干扰。(`a023b7d`) — **[自写]** —— 删了两份里程碑时代遗留 `SMOKE_TEST.md` / `KNOWN_UNCERTAINTIES.md`(MVP 阶段一次性产物,M5 收官后就不再维护,和现状脱节);`CHANGES_FROM_ROUBAO.md` 里"依赖变化"整段改成一行指回 `app/build.gradle.kts`(原来手写 enumeration 已经和真实依赖清单对不上 —— 飞书 oapi-sdk 2.4.16 / security-crypto / lifecycle-process / splashscreen 都漏了,再维护只会更偏),借鉴清单 + MIT 致谢保留不动;README 按现状重写 —— 去掉"M1-M5 开发进度"章节(已经全部完成,挂着反而让同事误以为还在 MVP),新增"架构一览"(nano/phone 两层 + gui_execute + 多 channel),功能章节铺开悬浮球、ClawguiIME、飞书 channel、trace、AES 加密、会话重命名、外部 inbox、Onboarding;技术栈补齐 Material 3 / oapi-sdk 2.4.16 / security-crypto / splashscreen / lifecycle-process;项目结构按当前源码树重画,`core/nano/{agent,bus,channels,providers,session,trace,cron,...}` + `core/phone/{actions,memory,model,platform,tracer}` + `core/ime/ClawguiIME.kt` + `ui/{overlay,screens,theme}` 全部补上;文档区链到 `docs/architecture-notes.md` / `docs/feishu-setup.md` / `CHANGELOG.md` / `CHANGES_FROM_ROUBAO.md`。纯文档改动,无代码影响。(`905e0d9`) — **[自写]**
- fix: Onboarding 首次打开黑底黑字 —— XML 层还是 dark palette 的遗留,light-only 主题迁移(436b327)只迁了 Compose。测试者描述"打开时黑背景黑字看不清"根因:`AndroidManifest` 走 `Theme.ClawGUI.Splash → postSplashScreenTheme=Theme.ClawGUI`,`themes.xml:9` 的 `android:windowBackground = @color/background_dark = #121212`(老 dark palette)没跟着迁。OnboardingScreen 的 Column 没 `.background()` 也没 Surface/Scaffold 兜底 → 透明 → 直接露出 Activity 的 windowBackground 深黑;Text 用 `MaterialTheme.colorScheme.onSurface`(ClawGUITheme light 值 `#141413`)→ 近黑字。深黑底 + 近黑字 ✓。MainApp 感觉不到是因为它外层 Scaffold 的 `containerColor = MaterialTheme.colorScheme.background` 把 `#FAF9F5` 铺满了,Onboarding 没铺,坑才露出来。修法 —— 让 XML 的 Activity 层和 Compose 主题口径对齐,未来任何没铺底的 Compose 屏(Dialog / BottomSheet / 半透明浮层)都不会再翻车:(1) `themes.xml` 的 `Theme.ClawGUI` parent 从 `Material → Material.Light`;`windowBackground` 从 `@color/background_dark` 换 `@color/app_background`(新增 `#FAF9F5`,值跟 `ClawGUITheme.colorScheme.background` 完全相等);`statusBarColor`/`navigationBarColor` 改透明,`windowLightStatusBar`/`windowLightNavigationBar` 翻 true —— 这些 XML 值只在 Activity 起来到 Compose inflate 那一帧 fallback,`enableEdgeToEdge` 在 onCreate 里会覆盖 statusBar/navigationBar style,透明 + light icon 的口径跟 edge-to-edge 状态一致避免闪;`colorAccent` 从 `@color/secondary` 改 `@color/primary`,单琥珀口径(Compose 那边也是单 primary accent)。(2) `colors.xml` 删 9 个无引用 dark palette 遗留色(`background_{dark,card,input}`、`primary_light`、`secondary{,_dark,_light}`、`text_{primary,secondary,hint}`,全部 grep 确认零引用);新增 `app_background = #FAF9F5`;`primary` / `primary_dark` 从 `#1976D2` / `#0D47A1` 蓝迁到 `#D97706` / `#B45309` 琥珀,跟 Compose primary / tertiary 对齐。`success` / `error` / `warning` 语义色保留,`overlay_text_*` 两个悬浮球专用色保留(overlay 背景自带 tint,白字独立配色)。(3) Splash 和 launcher icon 品牌蓝保留不动(`splash_background = #1565C0`、`ic_launcher_background = #1976D2`、foreground 是白色 vector 在蓝底可读),从 splash 到 Activity 之间会有一瞬蓝→cream 过渡,可接受;彻底统一需要换 foreground icon 颜色,是品牌改动,单独做。验证手段:`pm clear com.clawgui.android` 重置后冷启动应用,OnboardingScreen 三屏文字清晰可读,背景 cream。(`99ec6e2`) — **[自写]**
- fix: IME 弹起时输入框与键盘之间的 bottomBar 高度缝隙 + IME 收回时输入框触底 NavigationBar 晚回的时序错位 —— 同一个根因两个症状,一次收口。根因:ChatScreen 那套 `.imePadding()`(外层 Column,ChatScreen.kt:37)+ `.navigationBarsPadding()`(输入 Row 和 ReadOnlyFooter Surface)是 50d34fa 在"Scaffold 里还没 bottomBar"的年代加的,a3efad8 引入 NavigationBar 当 bottomBar 之后这套就跟 Scaffold 的 content padding 冲突了;38d1a95 只改 `adjustResize → adjustNothing` + `Scaffold(contentWindowInsets = WindowInsets(0))`,没清 ChatScreen 里那两行,还让 inset 消费链断了半截。具体冲突:IME 弹起时 Scaffold 已经把 bottomBar 的高度(~80dp)算在 `padding` 里传给 content,ChatScreen 外层 Column 又 `.imePadding()` 一次 —— content 底部 = screen_bottom − bottomBar_height − IME_height,IME 顶 = screen_bottom − IME_height,缝隙恰好 = bottomBar_height。IME 收回时更微妙:`.imePadding()` 跟着 `WindowInsets.ime` 的动画(~200ms)平滑缩,bottomBar 的高度重测依赖的是 `WindowInsets.navigationBars` inset,两条链路在 `contentWindowInsets = WindowInsets(0)` 切断消费之后不同步,中间有一帧输入框贴底、下一帧 NavigationBar 才回来,肉眼看就是"先触底再弹回"。修法:(1) `MainActivity.kt` 的 content Box 在 `.padding(padding)` 后补 `.consumeWindowInsets(padding)` —— 显式告诉下游"bottomBar 的 bottom inset 已经消费过了",之后 `.imePadding()` 就只应用 `max(ime − navigationBars, 0)` 的差值,输入框正好贴 IME 顶;(2) `ChatScreen.kt` 输入 Row(ChatScreen.kt:109)和 `ReadOnlyFooter` 里 Surface(ChatScreen.kt:156)上的 `.navigationBarsPadding()` 删 —— 现在 Scaffold bottomBar 坐在导航条上方,ChatScreen 的内容在 bottomBar 上面自然已经避开导航条,不需要再加一次,再加一次就是消费链上双计。外层 Column 的 `.imePadding()` 保留,`adjustNothing` + `contentWindowInsets = WindowInsets(0)` 保留,业务/状态零变化。顺手把 `MainActivity` 的 `imePadding` import 换成 `consumeWindowInsets`(imePadding 已经不在这边用了)。(`1288716`) — **[自写]**
- fix: 飞书回复超时兜底 + 飞书 turn 记 trace + 报错进 trace —— 两个互相独立的 bug 一次修。(1) 飞书任务跑完悬浮球一直转:根因 `FeishuChannel.send` 里 `client.im().message().reply(req)` / `create(req)` 是 oapi-sdk 的同步 OkHttp 调用,SDK 没加超时,网络被墙/代理卡住时协程无限阻塞,既不进成功路径也不进 catch → `onOutboundSent` 永不触发 → `releaseSlot` 永不调 → `_executionStatus` 卡 Running、overlay 一直转。修法:`withTimeoutOrNull(SEND_TIMEOUT_MS=30_000L)` 包整个 reply/create 调用,把原来单一布尔结果换成 `(ok, errDetail)` 元组,三条失败路径(`resp == null` 超时、`!resp.success()` API 错、`catch Exception`)全部触发 `onOutboundSent?.invoke(sessionKey, msg.content, errDetail)` 保证释放 slot;顺手修两个相关小 bug —— 原来 `resp.success() == false` 只 warning 一下就落到成功路径,用户飞书侧没收到但 App 以为完成,现在走失败分支;原来 `catch (e: Throwable)` 把 `CancellationException` 也吃了,拆成 `catch (e: CancellationException) { throw e } catch (e: Exception)` 不吞取消;失败时 **不** 写 `inbox.appendOutbound`(别假装发出去了)。`onInboundAccepted` 签名从 `(sessionKey) -> Unit` 扩到 `(sessionKey, content) -> Unit`,`onOutboundSent` 从 `(sessionKey, text) -> Unit` 扩到 `(sessionKey, text, error) -> Unit`,把原始文本和错误透到 App 让 trace 能记。(2) 飞书 turn 从来不记 trace、报错没处抓:原本只有应用内 `sendInstruction` 建 `TraceRecorder`,飞书路径 `onInboundAccepted` 只 acquireSlot 就跳进 AgentLoop,跑完 / 报错都不落 jsonl。`App.initChannels` 里两个 lambda 展开:入站 `acquireSlot` 成功后 **在同一分支里** 建 `TraceRecorder.create(channel="feishu", chatId=sessionKey.removePrefix("feishu:"), turnId="turn_$startMs")` 并 `record("user_input", ...)`,写进 `pendingTurns[sessionKey]` + `activeTurnRecorder = recorder`(slot 失败就不建 recorder 避免漏 release);出站 `pendingTurns.remove(sessionKey)?.let` 根据 error 记 `assistant_output` 或 `feishu_send_failed`(带 error 字段),`activeTurnRecorder` 指回自己时清,最后 `releaseSlot(sessionKey, text or "Error: 飞书发送失败 — $error")` + `ensureDisplayNameInternal(sessionKey)`。飞书 turn 从此也会在 `workspace/traces/<date>/turn_*.jsonl` 留痕,event 序列 `trace_started → user_input(source=feishu) → llm_request → tool_calls_planned → ... → assistant_output(channel=feishu)`。`stopSession` 原有的 `app_turn_cancelled` 对飞书 turn 天然适用,不用额外改。(3) 报错进 trace:`AgentLoop.dispatch` 的 `catch (e: Exception)` 原本只 `logger.log(SEVERE, ...)`,加一行 `traceRecorderProvider?.invoke()?.recordError(eventType = "dispatch_error", throwable = e, payload = mapOf("session_key", "channel", "chat_id"))`,异常类型 + 消息 + 堆栈写进当前 turn trace。`CancellationException` 分支不加 recordError —— 取消走 `stopSession → app_turn_cancelled` 已有事件,重复记没意义。复用 `TraceRecorder.recordError` 现成签名。改动收口在 `FeishuChannel.kt` + `App.kt`(initChannels lambda)+ `AgentLoop.kt`(catch 加一行),`InAppBridge`/`PendingTurn`/`InAppChannel`/`ChannelDispatcher`/`TraceRecorder` 不动。`uiSessionKey` 字段对飞书 turn 填同一个 sessionKey —— 这字段只写不读。(`580a961`) — **[自写]**

## 2026-04-21

- feat: 会话默认用第一句话命名 + 手动改名入口 —— 原本默认名字是 `MM-dd HH:mm` 时间戳,History 里一排时间戳看不出哪条在做什么。(1) `App.kt` 在 companion 加 `MAX_DISPLAY_NAME_LEN = 20`,新增 `deriveDefaultDisplayName(session)` 取第一条 `role=="user"` 的 message,trim + `\s+→空格` 压平 + 硬截 20 字;`sessionDisplayName` 和 `ensureDisplayNameInternal` 的 fallback 路径从 `formatTimestamp(createdAt)` 全部换成 `deriveDefaultDisplayName(session)`,空 session 仍走 `DEFAULT_NEW_NAME` 兜底;公开 `renameSession(key, newName): Boolean` —— 空字符串清 `metadata["display_name"]` 回到自动派生,非空 trim + 压平 + 截 20 后写 metadata + save,当前 session 同步 `_currentSessionDisplayName`,`bumpSessionsVersion()` 驱动 History 刷新,不受 `_isExecuting` 约束(执行中也能改)。顺手修飞书 session 从来没写过 display_name 的 bug:`initChannels` 里 FeishuChannel 的 `onOutboundSent` lambda 补一行 `ensureDisplayNameInternal(sessionKey)`,飞书出站成功后会把第一条 user 消息(飞书用户发的内容)写进 metadata,History/Inbox 里飞书会话从此不再是时间戳。unused 的 `formatTimestamp` + SimpleDateFormat/Date/Locale 三个 import 顺手删。(2) UI 两个改名入口:HistoryScreen 长按从"直接弹删除 AlertDialog"改成"ModalBottomSheet 菜单",菜单两项(✏️ 重命名 / 🗑 删除),state 从 `pendingDelete` 拆成 `pendingMenu`/`pendingRename`/`pendingDelete`,删除流程保持原样;MainActivity 的 Chat TopAppBar `actions` 从原来一个 + 图标拆成两个按钮 —— ✏️ Edit 图标走新 `showRenameDialog` state,`enabled = messages.isNotEmpty()`(空 session 禁改),Add 图标行为不变。新文件 `ui/screens/RenameSessionDialog.kt` 提供复用 `RenameSessionDialog(currentName, onDismiss, onConfirm)`:`AlertDialog` + `OutlinedTextField(singleLine, onValueChange { take(20) })`,placeholder "留空恢复默认",supporting text 显示 `X / 20` 字数计。两处入口共用同一个 Composable。(`4d55b64`) — **[自写]**
- feat: 飞书任务悬浮球 + 外部会话只读 —— 补阶段 2 两个行为缺口。(1) 飞书任务起来没悬浮球、也没停止入口:`App.kt` 新增 `_activeRunningSessionKey: StateFlow<String?>` 单槽状态 + `acquireSlot(sessionKey, stageHint)` / `releaseSlot(sessionKey, resultText)` / `stopSession(sessionKey)` 三个原语,in-app 路径的启动三连(`_isExecuting=true` + `_executionStatus=Running` + `startAgentService`)和释放三连合并成原语调用,`sendInstruction` 早退分支 / `stopInstruction` / `releaseExecution` 全部收敛过去;`FeishuChannel` 构造加 `onInboundAccepted`/`onOutboundSent` 两个可选 lambda,`onIncomingEvent` 白名单判定提前(否则非白名单触发会 acquire 却不 release,slot 挂住),白名单过后 invoke acquire,`send()` 成功 / rest client 未就绪 / catch 三条路径都 invoke release 避免任何分支漏掉释放。App 组装 FeishuChannel 时传 `{ k -> acquireSlot(k, "[飞书] 规划中…") }` + `{ k, t -> releaseSlot(k, t) }`,飞书消息从此驱动 `ExecutionStatus.Running` 走 AgentService 拉起 overlay;`stopSession` 里 `releaseSlot` 之后覆盖成 `Error("已停止")` 保持视觉一致(✗ 而不是 Done)。`stopInstruction` 简化为 `stopSession(activeRunningKey ?: currentKey)`。`InAppBridge.releaseExecution(text, isCurrent)` 签名改 `(sessionKey, text)`,InAppChannel.send 跟着调 `releaseSlot(sessionKey, text)`,语义一致化。(2) 外部会话在手机本地能发消息的 bug:`ChatScreen.kt` 加 `isReadOnly`/`canStopActive` 两个参数,底部 Row 拆成三分支 —— 非只读走原输入框 + 发送/停止;只读 + 活跃走 errorContainer 色 banner + "停止当前任务"按钮(onStop 复用 `app.stopInstruction`,stopInstruction 内部优先停 active session);只读 + 非活跃走 surfaceVariant 色 "来自外部 channel 的对话,仅可查看"。MainActivity 计算 `isReadOnlySession = sessionKey.isNotEmpty() && !sessionKey.startsWith("ui:")` + `canStopActiveSession = activeRunningKey == sessionKey` 传下去。副作用修复:`switchSession`、HistoryScreen/InboxScreen 的点击原本在 `_isExecuting=true` 时拦住,飞书任务跑起来用户根本进不去 session 按停止 —— 全部放开,只留 delete 按钮继续 gate(别边跑边删)。(`1cc1ac0`) — **[自写]**
- fix: 修复输入法弹起时白边和底部栏消失问题 —— light-only 主题 + SettingsScreen 重构落地后,IME 弹起会先由 Android framework 的 `adjustResize` 把 window 高度压掉一块,Compose 内部 `imePadding()` 再算一次偏移 —— 两套 inset 叠加,导致 Scaffold 边缘冒白边、底栏被整体顶出屏幕。AndroidManifest 把 MainActivity 的 `android:windowSoftInputMode` 从 `adjustResize` 改 `adjustNothing`,让 framework 不再吃 inset,Compose 自己的 `imePadding()` 独吞;Scaffold 额外补 `contentWindowInsets = WindowInsets(0)` 防止 Scaffold 默认把 system bar insets 又消费一遍(和 enableEdgeToEdge 路径撞车)。顺手做兼容性降级:M3 1.5.x BOM 下 `Icons.AutoMirrored.Filled.{ArrowBack,ChevronRight}` 在某些机型/编译 path 表现不一致,降回 `Icons.Filled.{ArrowBack,ChevronRight}`;`HorizontalDivider` 在 BOM 2023.10.01 里尚未稳定,统一换回 `Divider`(SettingsScreen 的 `IndexDivider` + Traces 子页中那一处)。仅影响渲染,不动任何业务/状态逻辑。(`38d1a95`) — **[同事 gta886]**
- feat: 悬浮球加 Mini 级别实现三级循环切换 —— Running 态默认进入 32dp 圆形 Mini spinner(仅一个转圈,无文字),点击循环 Mini → Compact → Expanded → Mini。`AgentOverlayManager.kt` 把原 `isExpanded: Boolean` 升级为 `DisplayLevel { Mini, Compact, Expanded }` 枚举,新增 `applyDisplayLevel(view)` 集中切三个容器可见性 + root 背景 + `lp.width/height/flags` + `updateViewLayout`,原 `toggleExpanded` 改 `cycleDisplayLevel`(守卫仅 Running 参与循环),原 `collapseIfNeeded` 改 `applyState` 内 Done/Error 分支直接 `displayLevel = Compact` 再调 `applyDisplayLevel`(Mini 圆形不兼容 ✓/✗ 文字所以强制升级)。`overlay_agent_status.xml` 在 `overlay_root` 顶部新增 `overlay_mini_container` FrameLayout(32dp 圆形背景 + 20dp spinner 居中),Mini 时 root 背景切透明避免外层又套方形;Compact/Expanded 时 mini GONE,root 按 `lastState` 走 `overlay_bg_running/done/error`。`dimens.xml` 加 `overlay_mini_size=32dp`、`overlay_mini_spinner_size=20dp`,新增 `drawable/overlay_bg_running_mini.xml`(半透明蓝圆形 oval)。touchSlop + 250ms 拖动/点击判定保留,拖动位置仍写回 `overlay_x/y` prefs 与旧行为共用。(`2a6abd7`) — **[自写]**
- feat: 切换 light-only 主题 + SettingsScreen 重构为主索引 + 子页 —— 配色整体换 Claude 奶白 `#FAF9F5` + 暖橙 `#D97706` 点缀,`Theme.kt` 删 ClawGUIDarkColors 只留 `ClawGUILightColors`;`MainActivity.enableEdgeToEdge` 的 statusBar/navigationBar style 从 `.dark(TRANSPARENT)` 改 `.light(TRANSPARENT, TRANSPARENT)`,浅色背景下系统栏文字才能走深色。SettingsScreen 整体重写:顶层 `SettingsSubPage` enum 列 6 个子页(AI 模型 / Channels / ClawGUI 输入法 / Shizuku / Trace 记录 / 关于),主索引走 iOS 设置风格 `IndexRow`(40dp primaryContainer 图标框 + 标题 + 动态摘要 + ChevronRight);子页内部改完字段底部 `Button("保存")` 生效,状态回调走 `onDirtyChange` 向父级上抛,保存走 `onSaved` 父级清 dirty + 回主索引。MainActivity 新增 `settingsSubPage`/`settingsDirty`/`showDiscardDialog` 三个 state 和 `tryBackFromSubPage` lambda,TopAppBar 的 `title` 在 Settings 里展示子页 displayName(无子页时展示"设置"),`navigationIcon` 在子页时显示 `Icons.AutoMirrored.Filled.ArrowBack`;`BackHandler(enabled = inSettingsSubPage)` 顶层拦系统返回走同一路径;切底栏 tab 离开 Settings 时顺手清 subPage/dirty 避免隐藏态;放弃对话框三步(放弃/取消/清 dirty + 回索引)。AiModels / Channels / Traces 三个子页内部字段改动会触发 dirty(`if (!dirty) onDirtyChange(true)`),Ime/Shizuku/About 只读无保存按钮。需要重启生效的子页(AI 模型、Channels)顶部用 tertiaryContainer Surface 显示"修改后需重启 app 生效。"。(`436b327`) — **[自写]** 完全基于 Material3 colorScheme 和 Compose state 自己搭,无借鉴
- feat: 接入飞书 channel —— 阶段 2。镜像 ClawGUI Python `channels/manager.py` 结构新增 `BaseChannel` / `ChannelDispatcher` / `InAppChannel`,把原 `App.handleInAppOutbound` 逻辑收进 InAppChannel,通过 `InAppBridge` 接口回调 App 改 UI state(in_app 行为 bit-for-bit 不变);outbound 消费由单例 `ChannelDispatcher` 按 `msg.channel` 路由到具体 channel(对齐 Python `_dispatch_outbound`),阶段 1 写在 App.kt 里的 outbound subscriber while 循环整块删除。新增 `FeishuChannel` 走 oapi-sdk-java 2.4.16 WS 长连 + `P2MessageReceiveV1` + REST `im().message().reply`(metadata 带 `feishu_message_id`,reply 回原 thread;拿不到时 fallback `create`),入站先过 `allowedOpenIds` 白名单(默认 off 最小权限),会话持久化由 SessionManager 自动处理(sessionKey = `feishu:<chatId>`)。`AgentLoop.processMessage` 3 处 OutboundMessage 把 `metadata = mapOf("sessionKey" to key)` 改成 `msg.metadata + mapOf("sessionKey" to key)`,让入站 metadata(含 feishu_message_id)透传回出站。`GUITool` 加全局 `Mutex` 设备互斥,第二个并发请求 tryLock 失败快速返回错误由 agent 作为 tool result 告诉用户(app 内 UI turn + 飞书 turn 同时跑只有一个能拿到设备)。新增 `ExternalInboxStore` 把外部 channel 的入/出站消息追加到 `workspace/external_inbox.jsonl`,带 `version: StateFlow<Long>` 让 UI 增量刷新。新增 `InboxScreen`(NavigationBar 第三项,原 Settings 后挪),倒序展示 200 条,点击一条 switchSession 到对应 feishu session 切 ChatScreen。`SettingsScreen` 加飞书配置卡(启用 / App ID / App Secret / 允许所有 open_id / 允许列表),App Secret 走 `EncryptedSharedPreferences`(security-crypto 已有依赖,剩余 key 仍在 plain prefs)。`App.onCreate` 注册 `ProcessLifecycleOwner` observer:onStart `dispatcher.start()`、onStop `dispatcher.stop()`(自带 ~30s 去抖,home 出去 channel 断、回前台重连)。build.gradle 加 `com.larksuite.oapi:oapi-sdk:2.4.16` + `androidx.lifecycle:lifecycle-process:2.6.2` + META-INF excludes(DEPENDENCIES/LICENSE/NOTICE,对齐 spike 分支实测值)。(`5fe518b`) — **[混合]** BaseChannel / ChannelDispatcher 结构借鉴 ClawGUI Python `nanobot/channels/{base.py,manager.py}`;oapi-sdk WS 接线借鉴同事 spike 分支 `dcca757`,其余 InAppBridge / ExternalInboxStore / GUITool 互斥 / 生命周期 wiring / Settings 和 Inbox UI 自写
- refactor: `App.sendInstruction` 改走 MessageBus —— AgentLoop 从"每 turn 新建"变单例常驻(`App.initAgentLoop` 里一次性注册 GUITool/ReadMemoryTool/WriteMemoryTool,`bus.run()` 长跑消费 inbound),发送侧只做 publishInbound + 把 turn 写进 `pendingTurns[sessionKey]`,回传侧起一个常驻 outbound 订阅器按 `metadata["sessionKey"]` 领取 turn 上下文写回 chat 气泡/状态/trace,stop 走 `AgentLoop.cancelSession(sessionKey)` 取代原先 `currentAgentJob.cancel()`。早 fail(缺 API Key / Shizuku 没连)依旧在 UI 线程直接返回,不走 bus 免得回绕。`AgentLoop.processMessage` 给 3 处 OutboundMessage 加了 `sessionKey` metadata,新增公开 `cancelSession`。`AgentLoop` / `GUITool` 的 `traceRecorder` 换成 `traceRecorderProvider: () -> TraceRecorder?`,App 侧用一个 `@Volatile activeTurnRecorder` 指向当前 turn,避免单例下 recorder 被钉死成 null(stage 1 保证同时只有一个 in_app turn,stage 2 feishu 并发到位再考虑 per-session map)。ChatScreen 外部行为零改动,为后续接入飞书等外部 channel 铺路。(`4e68a15`) — **[自写]**
- feat: SettingsScreen 顶部加重启提示条 —— 因为 AgentLoop 单例化后 Provider/VLM config 在启动时 build,改完 API key/model/provider 需要重启 app 才生效,用 tertiaryContainer Surface 放一行小字提示。(`4e68a15`) — **[自写]**

## 2026-04-20

- fix: Brain/VLM model 互踩 —— `SettingsStore` 原先 model key 只按 provider 分(`provider.$provider.model`),两张卡都默认 zhipu 时 VLM 保存会覆盖 Brain 的值,下一次 Brain 读出来是 `autoglm-phone`(视觉模型),对话/工具调度直接崩。拆成 `provider.$provider.brainModel` / `.vlmModel` 两套 key,API 对应拆成 `brainProviderModel`/`vlmProviderModel` + setter,SettingsScreen 跟着换新方法名;新增 `migrateRoleModelKeysIfNeeded()` 一次性把旧 `provider.zhipu.model` 迁到新 key —— brain 槽位跳过被污染的 `autoglm-phone` 回落到 `defaultModelHint`,vlm 槽位直接照抄,标记 `role_model_migration_v1_done` 防重复迁。顺手把 `ClawguiIME` 的 `registerReceiver` 在 TIRAMISU+ 上加 `RECEIVER_EXPORTED` flag,免得 Android 14 崩 (`fa95b88`) — **[同事 gta886]**
- feat: UI 美感二档 —— darkColorScheme 升级到 M3 tonal(primary 亮靛 `#B4C5FF`、primaryContainer 深靛 `#1A428A`、background/surface 收到 `#111318`,补 tertiary/outline/error container);MainActivity Scaffold 套 TopAppBar 按 Screen 派发 title + actions,ChatScreen/HistoryScreen 自画顶栏 Row 和 SettingsScreen "设置" headline 全删,ChatScreen onNewSession/sessionName 两个参数顺手去掉;Chat 气泡圆角 12→20,底部输入 `TextField`→`OutlinedTextField` + `RoundedCornerShape(24.dp)` 变 pill;SettingsScreen 所有 Card 套 `containerColor = surfaceVariant` + `elevation = 1.dp` + `shapes.large`,跟 background 分层更明显;图标轻处理:新增 `drawable/ic_launcher_background.xml` + `mipmap-anydpi-v26/ic_launcher{,_round}.xml` adaptive 架子(复用现有 foreground vector),AndroidManifest icon/roundIcon 引用挪到 `@mipmap/`,后续换 PNG/新 vector 都可以 (`a3efad8`) — **[自写]** 品牌 ClawGUI 保留不改(ClawGUI 下的一种实现,不是独立项目)
- feat: UI 视觉一致性收敛 —— M3 Typography/Shapes 补全(`ui/theme/Type.kt` + `Shapes.kt`,Theme 接两个新参数);清零 ChatScreen/HistoryScreen 里 `fontSize = *.sp` 两处硬编码(placeholder 改 `typography.bodyMedium`、"当前"标签改 `labelSmall`);step bubble `RoundedCornerShape(4.dp)` 挪到 `shapes.small`;overlay XML 的 textColor/textSize 全部挂 `@color/@dimen`,新增 `res/values/dimens.xml` 专放 overlay 尺寸,`colors.xml` 加 `overlay_text_primary/_label` (`76f2ec6`) — **[自写]** Typography/Shapes 值照 M3 默认规范,overlay_bg_*.xml 暂不碰(留给主题现代化档)
- feat: VLM 多 adapter —— 新增 ModelAdapter 接口(buildMessages/parseResponse/addHistory),port UI-TARS / Qwen-VL / MAI-UI / GUI-Owl 四家 prompt + adapter;tool_call / 函数调用 action 内部归一成 AutoGLM `do(action=..)` 让 ActionParser 通吃;PhoneAgent 走 adapter 工厂,每步 addHistory 支撑 Qwen / GUI-Owl 的"Previous actions"注入;Settings VLM 卡加预设 chips (`25ce76e`) — **[混合]** 四家 prompt 文本直接从 Python `clawgui-agent/phone_agent/config/prompts_*.py` port,adapter 归一化逻辑与接口拆分自写以绕开需要重写 ActionParser
- feat: Brain LLM 多 provider 支持 —— ProviderRegistry 扩到 15 家(zhipu/anthropic/openai/deepseek/moonshot/siliconflow/openrouter/dashscope/gemini/minimax/mistral/stepfun/volcengine/aihubmix/groq)+ backend 枚举;新增 AnthropicClient 走 /v1/messages 原生协议(messages/tools/image 三路翻译);SettingsStore 改 provider-keyed 存储并一次性 migrate 旧单 key;Settings 页拆 Brain / VLM 两张卡 (`a1ff27f`) — **[混合]** ProviderRegistry 条目与 AnthropicClient 字段结构借鉴 Python `clawgui-agent/nanobot/providers/{registry.py,anthropic_provider.py}`,SettingsStore 多 key 存储与 UI 卡片自写
- feat: 悬浮球可点击展开(Running 态),显示 VLM 当前步 thinking + action JSON;拖动/点击按 touchSlop 与 250ms 分离,Done/Error 自动折叠 (`187142e`) — **[自写]**
- feat: VLM thinking/action 透传到 ExecutionStatus/OverlayState,为悬浮球展开铺管道;GUITool.onProgress 签名扩到 4 参,AgentService 把字段透给 overlay (`ef936c3`) — **[自写]**
- feat: 自带 IME(ClawguiIME)—— Type 走 broadcast 直接 commitText,不再污染用户剪贴板;Shizuku 自动启用并任务级切换/还原,原剪贴板路径保留为 fallback;设置页加状态卡 + 一键启用 (`3fc057c`) — **[自写]** 仅 BroadcastReceiver 三条 action 的接线思路借鉴 ADBKeyboard
- feat: Type 动作污染剪贴板的问题,惰性还原(首次 Type 前保存,下一个非 Type 动作前还原,任务结束兜底) (`9fcab47`) — **[自写]**
- fix: ChatScreen 输入框不再被软键盘 / 导航栏遮挡 (`50d34fa`) — **[合并 wxf]** 借鉴 `914469f` 两行 Modifier 改动
- feat: 每轮对话写 trace jsonl,设置页新增 Trace 开关和最近 12 条查看器 (`e94b397`) — **[混合]** TraceRecorder/TraceUtils/SettingsScreen Trace UI/GUITool & AgentLoop 的 hook 内容借鉴 wxf `1efb0ce`;App.kt 里 TraceRecorder 的接线因主分支不走 Router 而自写,事件名与 payload 对齐 wxf
- feat: Launch 对 app 名字的识别率提升 —— 同义词表扩充 + PackageManager 实装 app 列表 + 注入大脑 prompt (`525de10`) — **[自写]**

## 2026-04-19

- docs: 核心组件改动前的注意事项 (`0465c84`) — **[自写]**
- feat: read_memory / write_memory 工具 + 对齐 system prompt (`0ac637f`) — **[自写]**
- feat: 多会话持久化 + 新建 / 切换 / 删除 + 修复 processDirect 忽略 sessionKey (`a4fb626`) — **[自写]**
- fix: cancel 时把被中止的请求写入 session,避免后续指代性指令误回溯 (`0a119ad`) — **[自写]**

## 2026-04-18

- feat: 全局执行状态悬浮窗 + phone_agent 步骤进度上报 (`f72f8e7`)
- fix: 大脑 prompt 修正,避免普通对话被误判成 GUI 指令 (`eafb151`)
- feat: 把 nanobot AgentLoop 接入 App.sendInstruction (`13cc9fc`)
- fix: 默认 VLM 模型改为 autoglm-phone,新增大脑模型配置 (`b7e6630`)
- fix: 解决全部构建错误,改进 API 错误可见性 (`8e41908`)
- chore: 初始 commit,ClawGUI Android MVP (`82f5074`)
